#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<vector<string>> partition(string s) {
        
    }
};

int main() {
    Solution s;
    string str = "aab";
    vector<vector<string>> ans = s.partition(str);
    cout << endl;
    return 0;
}